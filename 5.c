#include<stdio.h>

int main() {
	//문자를 저장하는 변수
	//char a; //char형 변수 선언
	//a = 'a'; // 모든 문자들은 모두 작은 따옴표로 묶어 주어야 한다.

	//printf("a 의 값과 들어 있는 문자는?\n값 : %d \n문자 : %c \n", a, a);
	//return 0;

	//Scanf의 도입
	/*double celsius;*/
	/*for (;;) //반복문과 조건문의 활용
	{*/
		/*printf("섭씨 온도를 화씨 온도로 바꿔주는 프로그램 입니다. \n");
		printf("섭씨 온도를 입력해 주세요 :");
		scanf_s("%lf", &celsius);

		printf("섭씨 %f 도는 화씨로 %f 도 입니다. \n", celsius, 9 * celsius / 5 + 32);*/
		/*if (celsius >= 300) {
			break;
		}
	}*/

	//scanf 총정리
	/*char ch;
	
	short sh;
	int i;
	long lo;

	float fl;
	double du;

	printf("char 형 변수 입력 : ");
	scanf_s("%c", &ch);

	printf("short 형 변수 입력 : ");
	scanf_s("%hd", &sh);
	printf("int 형 변수 입력 : ");
	scanf_s("%d", &i);
	printf("long 형 변수 입력 : ");
	scanf_s("%ld", &lo);

	printf("float 형 변수 입력 : ");
	scanf_s("%f", &fl);
	printf("char 형 변수 입력 : ");
	scanf_s("%lf", &du);

	printf("char : %c, short : %d, int : %d", ch, sh, i);
	printf("long : %d, float : %f, double : %f\n", lo, fl, du);*/

	return 0;
}